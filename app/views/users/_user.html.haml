%li
  = gravatar_for user, :size => 30
  = link_to user.name, user
  - if current_user.admin?
    |
    = link_to "delete", user, :method => :delete, :confirm => "Your share ?", :title => "Delete #{user.name}"